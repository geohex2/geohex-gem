# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'geohex' do
  # developer('FIX', 'FIX@example.com')

  # self.rubyforge_name = 'geohexx' # if different than 'geohex'
end

# vim: syntax=ruby
